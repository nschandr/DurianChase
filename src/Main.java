import processing.core.PImage;

import java.util.List;

public class Main extends Entity{
    public Main(String id, Point position, List<PImage> images) {
        super(id, position, images);
    }
}
